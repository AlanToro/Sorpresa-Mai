<!DOCTYPE html>
<html>
<head>
    <title>Acceso con Contraseña</title>
</head>
<body>
    <h2>Por favor, ingrese su contraseña</h2>
    <input type="password" id="password" placeholder="Ingrese su contraseña">
    <button onclick="verificarPassword()">Acceder</button>

    <script>
        function verificarPassword() {
            var password = document.getElementById("password").value;
            var contraseñas = {
                "123456": "https://enlace1.com",
                "abcdef": "https://enlace2.com"
            };
            
            if (contraseñas[password]) {
                window.location.href = contraseñas[password];
            } else {
                alert("Contraseña incorrecta");
            }
        }
    </script>
</body>
</html>
